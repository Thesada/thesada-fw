.DS_Store
*.patch
CLAUDE.md
AGENTS.md

# PlatformIO
.pio
.env
build/

# pioarduino / IDF component manager + per-env kconfig cache
# sdkconfig.defaults IS tracked (carries mbedtls overrides for CYD env).
# Per-env sdkconfig.<env> files are IDF output, regenerate on clean build.
managed_components/
.dummy
sdkconfig.esp32-*
CMakeLists.txt

# VSCode (PlatformIO-generated)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Device-specific runtime data (gitignored, flashed separately)
data/config.json
data/scripts/**.lua
data/ca.crt

# Test artifacts
tests/__pycache__

# Deploy script (contains server details)
scripts/deploy-ota.sh
